---
all:
  children:
    # FABRIC group drives AVD configuration generation
    FABRIC:
      children:
        SPINES:
          hosts:
            spine1:
              ansible_host: 172.20.20.11
        LEAFS:
          hosts:
            leaf1:
              ansible_host: 172.20.20.12
            leaf2:
              ansible_host: 172.20.20.13

    # NETWORK_SERVICES contains nodes that will have tenant VLANs/VRFs
    NETWORK_SERVICES:
      children:
        LEAFS:
